# ParadeDB image for CNPG

This just builds the latest postgres 18 paradedb image and changes the postgres user id to 26 as that is what CNPG Cluster requires.

The image is also labelled so that CNPG understands the postgres version.